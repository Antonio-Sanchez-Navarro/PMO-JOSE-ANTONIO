{
  // Build CommonJS: es lo que consume la API (NestJS con `require`).
  // El ESM para el frontend sale de `tsconfig.esm.json`.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist/cjs",
    "rootDir": "src"
  },
  "include": ["src/**/*"]
}
